#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
/* print lowercase letters */
		putchar(c);
	for (c = 'A'; c <= 'Z'; c++)
/* print uppercase letters */

	putchar('\n');

	return (0);
}
